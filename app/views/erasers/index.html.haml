%h1 Erasers

= form_for @eraser, :class => "form-inline"  do |f|
  %p register twitter user as your eraser.
  .form-group
    .input-group
      .input-grpup-addon @
      = f.text_field :twitter_name
      .input-group-btn
        %button.btn
          %i.icon-plus
          add


%table.table.table-striped
  %tr
    %th Twitter Name#ID
    %th Function

  - @erasers.each do |eraser|
    %tr
      %td
        = "#{eraser.twitter_name}##{eraser.twitter_id}"
      %td
        = link_to "Delete", eraser, method: :delete, data: { confirm: "Are you sure?" }
